const bookToPlainText = (plainTextString) => {
    let params = plainTextString.split("#")
  return {
    title: params[0],    
    author: params[1]
    
    title: params[0]
    
    title: params[0]
    
    title: params[0]
    
    title: params[0]
    
    title: params[0]
  };
};

export default bookToPlainText;
